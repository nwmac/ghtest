name: zube-sync
on:
  issues:
    types: [ closed ]

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: script
      run: .github/workflows/scripts/pr.sh "${{ secrets.GITHUB_TOKEN }}"
